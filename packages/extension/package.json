{
  "name": "@bugcase/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "vite",
    "dev:firefox": "cross-env BROWSER=firefox vite",
    "build": "cross-env BROWSER=chrome vite build && cross-env BROWSER=chrome vite build --config vite.content.config.ts",
    "build:chrome": "cross-env BROWSER=chrome vite build && cross-env BROWSER=chrome vite build --config vite.content.config.ts",
    "build:firefox": "cross-env BROWSER=firefox vite build && cross-env BROWSER=firefox vite build --config vite.content.config.ts",
    "build:all": "pnpm run build:chrome && pnpm run build:firefox",
    "lint:firefox-manifest": "web-ext lint --source-dir=dist-firefox",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bugcase/schema": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@types/webextension-polyfill": "^0.12.1",
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "cross-env": "^7.0.3",
    "jsdom": "^29.1.1",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.10",
    "vitest": "^2.1.4",
    "web-ext": "^8.3.0"
  }
}
